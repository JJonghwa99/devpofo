## 주요 기능

* 관리자 승인 기반 회원가입
* 권한 분리
* 성적 관리 및 시각화
* 시험 해설 게시판
* 익명 Q&A 게시판
* 공지사항

---
---
## 아키텍처
![SOOMATH 아키텍처](/images/architecture/soomath_architecture.svg)
---

## 기술 스택

* **Backend**: Java 21, Spring Boot, Spring Web, Spring Data JPA, Spring Security
* **Frontend**: Thymeleaf, HTML, CSS, JavaScript
* **Database**: PostgreSQL (Supabase 활용)
* **Rich Text Editing**: CKEditor 5
* **Server-side Charting**: JFreeChart
* **Build Tool**: Gradle

### 선택 이유

* **CKEditor5**: 수식과 이미지가 포함된 문제/해설을 서식 있게 작성할 수 있는 WYSIWYG 에디터가 필요해 도입
* **JFreeChart**: 별도 프론트엔드 차트 라이브러리 없이 서버에서 바로 이미지로 성적 그래프를 생성·저장할 수 있어 채택
* **Thymeleaf**: Spring Boot와 통합이 자연스러운 서버사이드 템플릿 엔진이라 빠르게 화면을 구현하기 위해 선택
* **Naver Cloud Platform**: 신규 가입 시 일정 기간 무료로 서버 인스턴스를 제공해, 비용 부담 없이 실제 클라우드 환경에서 서비스를 운영해볼 수 있어 선택
* **Supabase**: PostgreSQL 기반 매니지드 DB를 무료로 제공하면서 콘솔에서 손쉽게 관리할 수 있어 도입

---

## 핵심 구현 사항

### 문제 해결 경험

* **문제점**
  * 서비스 운영 중 서버 응답이 간헐적으로 멈추는 장애 발생. 클라우드 DB인 Supabase를 재시작하면 일시적으로 복구되었으나 근본 원인은 해결되지 않음.
* **원인 분석**
  * 애플리케이션의 요청 및 응답 로그를 분석한 결과, DB와 통신하는 주요 비즈니스 로직에서 자원 반환 처리가 누락되어 데이터베이스 커넥션 풀이 고갈되는 것을 확인. 단순 조회 작업에서도 커넥션을 불필요하게 장기 점유하며 메모리를 낭비하고 있었음.
* **해결 과정**
  * 작업 단위별로 트랜잭션 경계를 명확히 재설정. 특히 읽기 전용 작업에는 사용 후 즉시 커넥션이 반환되도록 로직 개선.
* **결과**
  * 한정된 자원 내에서도 서버가 장애 없이 안정적으로 운영되도록 개선.

### 관리자 승인 기반의 회원 관리 및 권한 제어

* Spring Security를 활용하여 ROLE 기반 '원생'과 '관리자'의 권한 분리.
* **가입 승인 절차**
  * 사용자가 회원가입을 신청하면 데이터는 `TempMember` 엔티티에 저장.
  * 관리자가 관리자 페이지에서 가입 신청 목록을 확인하고, 승인/거부 처리 가능.
  * 승인된 회원의 데이터는 `Member` 테이블로 이전되어 서비스의 핵심 기능에 접근할 수 있도록 설계.
* **접근 제어**
  * SecurityConfig와 `@PreAuthorize` 어노테이션 등을 사용, 성적 조회, 게시판 이용 등 원생 전용 기능과 회원 승인, 공지사항 작성 등 관리자 전용 기능의 접근을 URL 및 서비스 메서드 레벨에서 통제.

### JFreeChart를 활용한 성적 데이터 시각화 및 관리

* 서버 사이드 차트 라이브러리인 `JFreeChart`를 사용해 분반별 학생들의 최고점·최저점·평균 점수를 계산하고, 해당 학생의 점수가 분반 내 어느 위치에 있는지 그래프로 시각화하는 기능을 구현.
* **역할 분리 컨트롤러**
  * 원생용 `StudentScoreController`와 관리자용 `AdminScoreController`를 분리하여 API 설계.
* **차트 생성 로직**
  * ScoreService에서 성적 데이터 업로드 시, 평균을 자동 계산, 이 데이터를 바탕으로 JFreeChart 객체 생성.
* **동적 차트 제공**
  * 원생은 자신의 '분반별/월별' 성적 추이 그래프 조회 가능.
  * 관리자는 각 원생의 성적 그래프 확인 및 '해당 성적 그래프'를 이미지 파일로 저장 가능.

### CKEditor 기반의 인터랙티브 게시판 구현

* **시험 해설 게시판 (`ExamController`)**
  * 블로그 형식의 UI를 제공, 복잡한 수식이나 문제 풀이 과정을 효과적으로 전달할 수 있도록 이미지/영상 첨부 기능 구현. 페이지네이션 로직 적용.
* **Q&A 게시판 (`QnaController`)**
  * 학생들이 심리적 부담 없이 질문할 수 있도록 익명 질문 기능 핵심.
  * 답글 기능 추가하여 추가질문이 생겨도 이어서 질문 가능.
* **공지사항 (`NoticeService`)**
  * 메인 화면 진입 시 항상 최신 공지를 확인할 수 있도록 UI 구성.


---
